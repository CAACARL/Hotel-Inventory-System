-- Create database for Icon Venue & Suites Inventory Management System
CREATE DATABASE IF NOT EXISTS hotel_inventory CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Create a user for the application (optional)
-- CREATE USER 'hotel_user'@'localhost' IDENTIFIED BY 'hotel_password';
-- GRANT ALL PRIVILEGES ON hotel_inventory.* TO 'hotel_user'@'localhost';
-- FLUSH PRIVILEGES;

USE hotel_inventory;